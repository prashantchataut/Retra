# Retra 0.7.0 verification evidence

Date: 2026-07-19

## Implemented and reviewed in source

| Area | Evidence |
|---|---|
| UPS/BPS inspect + CRC helpers | `core/patching/.../PatchEngine.kt`, `PatchEngineTest` |
| Managed imports `.gba/.zip/.ups/.ips/.bps`, `.nds` reject | `GameRepository.kt`, `ImportModels.kt` |
| Guided pending patch UI | `RetraViewModel` + `PendingPatchSheet` in `RetraUi.kt` |
| Room v5 + MIGRATION_4_5 | `RetraDatabase`, `AppModule`, `GameEntity` |
| Catalog policy GBA/ZIP/patch + EXTERNAL block | `CatalogDownloadPolicy`, `CatalogDownloadPolicyTest` |
| Curated / official / pinned Discover groups | `CatalogRepository.curatedLinks`, `CuratedReleaseRepository`, Discover UI |
| External intents | `AndroidManifest.xml`, `MainActivity.importFromIntent` |
| Four destinations + filters + onboarding | `RetraUi.kt`, `OnboardingUi.kt` |
| Theme / mark | `RetraTheme.kt`, `ic_retra_foreground.xml`, `MASTER.md` |
| CI / mGBA packaging | `.github/workflows/build-release.yml`, `scripts/fetch-mgba-archive.sh`, `scripts/build-mgba-libretro-android.sh` |
| Version | `app/build.gradle.kts` → 8 / 0.7.0 |

## Local execution note

The authoring workstation’s JDK install is missing `lib/modules`, so local Gradle could not run unit tests or assemble the APK. Verification of compile, unit tests, lint, mGBA staging, and APK packaging is expected from the GitHub Actions workflow after push to `main`.

## UI checklist (source-level)

Reviewed for presence of:

- Compact bottom nav labels (Home / Library / Discover / Settings)
- Rail path for wider layouts
- Content width cap (~900dp)
- Dark/light theme tokens
- Reduced motion / reduced transparency settings
- 48dp-friendly primary actions
- Accessibility content descriptions on key icons/actions

Device screenshots still required after installing the CI APK.

## Explicit non-claims

- Heart & Soul patch binary is not bundled
- Commercial ROMs are not bundled
- Release APK is debug-signed for sideload only
