# Retra full redesign plan (historical)

This plan is complete for the Operate / Archive Glass shell.

**Current UI:** see [`docs/UI_MAP.md`](UI_MAP.md). There is one product shell (`RetraApp`); versioned V1/V2/V3 dialects are gone.

## Outcomes delivered

- Dead alternate shell removed
- Theme shapes / `RetraPanel` component kit
- Operate-first Home / Library / Discover / Profile
- Immersive player chrome defaults
- Focused UI files under `app/.../emulator/` (no parallel product roots)
